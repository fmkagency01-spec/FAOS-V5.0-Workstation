# Public frontend config (safe to expose in the browser)
NEXT_PUBLIC_API_URL=https://faos-backend.onrender.com
NEXT_PUBLIC_APP_ENV=production

# Server-only secrets — set these in the Vercel project settings, never commit real values
OPENROUTER_API_KEY=
